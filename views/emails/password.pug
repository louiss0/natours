extends baseEmail

block content
    p Hi #{firstName}
    p Forgot your password. Please send your new password and confirm it
    p Go to #{url}. If not please ignore this email
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td
                                    a(href=url, target='_blank') Upload user photo